import { Header } from "../Header";
import { render, screen } from "@testing-library/react";

describe("Header component redners properly", () => {
  it("only renders login and register links if there is no current user", () => {});
});
